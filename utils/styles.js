
const selectedButtonStyle = 'bg-blue-600 dark:bg-blue-600'
const notSelectedButtonStyle = 'bg-blue-100 dark:bg-gray-600'

const chevronIcons = 'text-black-500 dark:text-white'

const disclosurePanel = 'bg-blue-100 dark:bg-gray-900'

const disclosureButtonStyle = 'bg-blue-100 text-black-900 hover:bg-blue-200 focus-visible:ring-blue-500 dark:bg-gray-600 dark:text-white dark:hover:bg-gray-500 dark:focus-visible:ring-gray-500'

const buttonStyle = 'bg-blue-700 text-white dark:bg-blue-800 dark:text-white'

const spanBgStyle = 'bg-white dark:bg-gray-600'

const checkStyle = 'text-blue-600 dark:text-blue-400 dark:font-weight:bolder'

const bodyStyle = 'bg(gray-100 dark:gray-900) dark:text-gray-100 h-full flex flex-col'

const inputsStyle = 'bg-white dark:bg-gray-600'

const thStyle = 'bg-blue-400 dark:bg-gray-700 dark:text-white'

const row1Style = 'bg-blue-200 dark:bg-gray-600'

const row2Style = 'bg-blue-100 dark:bg-gray-500'

const linkStyle = 'color:blue; dark:color:blue; dark:font-weight:bolder;'

const dateInputStyle = 'color-scheme: light dark;'

export { selectedButtonStyle, notSelectedButtonStyle, disclosureButtonStyle, chevronIcons, disclosurePanel, buttonStyle, spanBgStyle, checkStyle, bodyStyle, inputsStyle, thStyle, row1Style, row2Style, linkStyle, dateInputStyle };